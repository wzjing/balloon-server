extends layout
block head
  title= title
  link(rel='stylesheet', href='/stylesheets/index.css')

block content
  .toolbar
    img.toolbar-logo(src='/images/logo.svg')
    a.toolbar-title(href='#') Balloon
    a.login-button(href='/Login') LOGIN
    .menu-group
      a.menu(href='/user') User
      a.menu(href='/express') Express
      a.menu(href='/service') Service
      a.menu(href='/about') About